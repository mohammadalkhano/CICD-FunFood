const homeView = "/"
const aboutView = "/about"
const contactView = "/contact"
const recipesView = "/recipes"

const RoutingPath = {
    homeView,
    aboutView,
    contactView,
    recipesView,
}

export default RoutingPath

