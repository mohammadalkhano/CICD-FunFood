const homeView = "/"
const aboutView = "/about"
const contactView = "/contact"
const resipesView = "/resipes"

const RoutingPath = {
    homeView,
    aboutView,
    contactView,
    resipesView,
}

export default RoutingPath

