import { Recipe } from '../recipesView/recipe/Recipe';
import { Enzyme, shallow } from 'enzyme';
import Adapter from '@wojtekmaj/enzyme-adapter-react-17';

describe('integration', () => {
  it('Recipe', () => {
    // const recipe= shallow(<Recipe />);
    expect(1).toBe(1);
  });
});
